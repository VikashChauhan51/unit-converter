<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:components="clr-namespace:UnitConverter.Components"
             x:Class="UnitConverter.Components.LessSuckyPicker">
    <ContentView.Content>
        <Frame BorderColor="{StaticResource PrimaryGray}" Padding="0" Margin="0, 10, 0, 0" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="1" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>

                <components:BorderlessPicker x:Name="borderlessPicker" Grid.Row="0" Grid.Column="0" HorizontalOptions="FillAndExpand" VerticalOptions="Center" Margin="10, 0, 10, 0"  />
                <Button Grid.Row="0" Grid.Column="1" x:Name="btn" Image="down_arrow.png" VerticalOptions="Center" HorizontalOptions="Center" BorderRadius="0" BorderWidth="0" BackgroundColor="Transparent" Margin="0" WidthRequest="16" />

            </Grid>

        </Frame>
    </ContentView.Content>
</ContentView>